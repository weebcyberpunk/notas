# Python3 - Geral

# *antes de mais nada, se precisar de ajuda, não hesite em digitar help() no interpretador, ou ainda colocar o elemento que tem dúvida nos parênteses 

# Anotações gerais:
    # O Python funciona de cima pra baixo e da esquerda pra direita
    # Se usar dois comandos na mesma linha, ele faz o primeiro antes, e o segundo só é feito depois
    # O Python possui as seguintes variáveis:
        # str() -> string. É texto
        # int() -> número inteiro
        # float() -> número decimal
        # bool() - booleana, binária, pode ser verdadeiro ou falso
# Strings são escritas entre " " ou ' '
# = faz um objeto antes do = adquirir o valor após
# == é o igual
# != é diferente
# # cria um comentário no meio do código, um texto que não será processado no programa
# Strings são imutáveis. Para mudar uma variável precisa redefinir ela
# Variáveis podem ser listas. Listas são definidas como [x, y, z]


# Comandos:
    print()
        # escrever o que está nos parênteses
    = input()
        # variável = o que você escrever. Dentro do parêntese aparece o "pedido" para que escreva
    import
        # deixa usar mais comandos de mais bibliotecas
    del
        # apaga a variável da memória
    help()
        # ajuda interativa do python
    return
        # retorna valor (ver return em funções)

# Comandos 2:
    .is…
        # mostra se a variável antes do ponto é algo (ex: x.isalpha, y.isnumeric)
    end=""
        # especifica como será o fim de um print e coloca o print da próxima linha logo em seguida
    \n
        # quebra a linha no meio do print. Inserir dentro da string
    from
        # usar "from (nome da biblioteca) import". Seleciona quais comandos da biblioteca importar. Isso faz com que não seja necessário usar biblioteca.comando. Se usar "from biblioteca import *", importará tudo
    in
        # ver se algo está em algo
    not in
        # intuitivo
    .center(x)
        # centraliza um print considerando x como o número total (centro = metade)


# Palavras reservadas:
    False
    None
    True
    and
    as
    assert
    break
    class
    continue
    def
    del 
    elif 
    else
    except
    finally
    for
    from
    global
    if
    import
    in
    is
    lambda
    not
    nonlocal 
    or
    pass
    raise
    try
    return
    while
    with
    yield
