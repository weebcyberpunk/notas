# Python3 - Tuplas

# Tuplas são variáveis compostas, ou seja, guardam vários valores

# Tuplas são imutáveis, não tem como tirar um elemento de uma tupla da tupla

# Tuplas são escritas entre (). Da pra atribuir o valor da variável como sendo uma tupla sem escrever com parênteses, exemplo:
    sobremesa = ("pudim", "paçoca")
    print(sobremesa)

        # Resulta em:
            ("pudim", "paçoca")

    # Assim como:
        sobremesa = "pudim", "paçoca"
        print(sobremesa)

            # Resulta em:
                ("pudim", "paçoca")

# Funcionam parecidas com strings, o fatiamento é igual

# Juntando tuplas com +, cria-se uma tupla formada pelos elementos da primeira e em seguida os elementos da segunda

# Comandos:
    len()
        # quantidade de elementos da tupla dentro dos parênteses
    max()
        # o maior valor dentro da tupla nos parênteses
    min()
        # mesma coisa que max() porém o menor

# Comandos 2:
    .index()
        # mostra em que posição o elemento dentro do parêntese está na tupla antes do ponto. Usando (elemento, x) ele mostra a partir da posição x
