import javax.swing.*;

// É evidente que, antes de aprender a lidar com eventos em GUI, precisamos aprender a lidar com eventos em geral

public class EventosDeUI extends JFrame {
	/*
	 * A superclasse de todo evento em AWT/Swing é AWTEvent
	 * Além de filha de EventObject, possui o método:
	 */
	int getID();
	/*
	 * Usado para identificar o tipo de evento disparado.
	 *
	 * O pacote java.awt.event define vários eventos disparados por interações em interfaces
	 */	
}
