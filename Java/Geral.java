package notas;

/**Isso é um comentário de documentação
 * É possível fazer marcações com ele:
 * 
 * @author GG weebcyberpunk@gmail.com
 */

public class Geral {
    public static void main(String[] agrs) {
        /* LEMBRE-SE DE SEPARAR AS LINHAS COM ;
         * 
         * Pois bem, comentários já foram mostrados
         * 
         * 
         * Notas gerais:
         * 
         * Java sempre faz diferença entre maísculas e minúsculas
         * 
         * Sempre que definir uma classe, as primeiras letras de cada palavra são maísculas:
         * MyClass
         * 
         * Sempre que definir um parâmetro, atributo, váriavel ou método, a primeira letra minúscula, porém as outras não:
         * myParameter
         * myAtribute
         * myVariable
         * myMetod
         * 
         * Sempre que definir um pacote, tudo minúsculo:
         * mypackage
         * 
         * Sempre que definir constantes, tudo maísculo:
         * MYCONSTANT 
         */

        // Comandos em geral:
        System.out.print(""); // print normal
        System.out.println(); // print pulando linha
        System.out.printf("Ex: %2f, %s \n ex", 0.555, "ex"); // print formatado
        /* Perceba que, nesse print, o que será mostrado é isso:
         * Ex: 0.55, ex
         * ex
         */
        System.exit() // encerra a JVM. Perceba que parâmetros inteiros serão retornados ao sistema operacional
        
         // Mais comandos:
         public // Declarar objeto como público
         private // Declarar objeto como privado
         public class // Declarar classe (pública, nesse caso)
         public class Etc extends // extends serve para que o objeto herde características de outro objeto
         public class Etc implements // para que a classe implemente uma interface
         new // Para criar objetos com estrutura nova

    }
}
