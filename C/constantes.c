// Constantes são declaradas fora de funções e são potencialmente utilizadas por várias delas
// Funções e constantes com o mesmo nome são a mesma coisa em qualquer arquivo, mesmo que sejam compilados separadamente. 
// Qualquer função pode acessar uma constante chamando seu nome, se ele tiver sido declarado em algum lugar
// Constantes são FODAS. Váriaveis internas a uma função só existem enquanto a função estiver rodando. As constantes existem pra sempre 

// Constantes são declaradas assim:
#define PI 3.14

// Muito simples

// Não tenho mais nada a declarar

int main() {

	return(0);
}
