#include <stdio.h>

int main() {
  /*
   * Operadores aritméticos:
   *
   * + soma
   * - subtração
   * * multiplicação
   * / divisão
   * % resto da divisão
   *
   * Perceba que a operação feita é inteira se os dois valores são inteiros. Caso um deles seja flutuante, o outro será automaticamente convertido.
   */

	return(0);
}
