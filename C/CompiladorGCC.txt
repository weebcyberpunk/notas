O compilador gcc é o compilador GNU de C

Para compilar, é simples:

  $ gcc myprogram.c
  
Se o processo terminar sem retorno, parabéns, a compilação foi um sucesso

Para compilar e receber um arquivo com outro nome que não a.out:

  $ gcc -o MyProgram myprogram.c
